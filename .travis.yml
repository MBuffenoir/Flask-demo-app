sudo: required

language: python

services:
  - docker

before_install:
- docker pull lalu/flask-demo-app
- docker run -d --name app lalu/flask-demo-app
- docker ps -a

script:
- docker exec app python -m unittest discover
